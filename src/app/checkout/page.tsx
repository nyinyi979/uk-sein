import { Metadata } from "next";
import Checkout from "./components/checkout";

export default Checkout;

export const metadata: Metadata = {
  title: "Checkout",
  description: "Checkout the items that you have decided to buy!",
};
