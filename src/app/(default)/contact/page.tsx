import { Metadata } from "next";
import Contact from "./components/Contact";

export default Contact;

export const metadata: Metadata = {
  title: "Contact",
  description: "Many possible ways to reach out to UK SEIN!",
};
