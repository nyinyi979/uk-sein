import { Metadata } from "next";
import Orders from "./components/Orders";

export default Orders;

export const metadata: Metadata = {
  title: "Orders",
  description: "Orders that you have made.",
};
