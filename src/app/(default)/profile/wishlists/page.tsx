import { Metadata } from "next";
import WishList from "./components/wishlists";

export default WishList;

export const metadata: Metadata = {
  title: "Wishlist",
  description: "Items that are your favorites.",
};
