import { Metadata } from "next";
import Products from "./components/Product";

export default Products;

export const metadata: Metadata = {
  title: "Products",
  description: "Different products available in UK SEIN!",
};
