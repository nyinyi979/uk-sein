import { Metadata } from "next";
import Portfolio from "./components/Portfolio";

export default Portfolio;

export const metadata: Metadata = {
  title: "Portfolio",
  description: "What we have accomplished during the past years!",
};
